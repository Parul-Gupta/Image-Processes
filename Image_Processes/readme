To run the code, ensure that your directory has the standard OpenCV libraries, use a makefile.
To run the code, type the following commands:
						cmake .
						make
						./Image_Processes <input image path>
